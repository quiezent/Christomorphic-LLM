# Research Status

Status date: **2026-08-16**

## Executive Read

The project has moved from checkpoint discovery and composed behavioral systems into controlled causal formation research.

The strongest faithful summary is:

> The methodology has advanced from surface behavior toward prospective mechanism tests, but no current model has demonstrated durable Christ-specific causal formation or scaffold-off public governance.

No branch, checkpoint, adapter, local state, teacher object, or experiment is currently promoted as a Christomorphic model.

## Two Bridges Still Unproved

The current program separates the end goal into two non-interchangeable bridges.

| Bridge | Required result | Current status |
|---|---|---|
| **Formation bridge** | Exact Scripture and canonical judgment cause a durable, scaffold-off, Christ-specific change in slow weights and internal computation | Not proved |
| **Governance bridge** | The formed state governs bare FIRST_ACT, stayed continuation, faithful action, safety, and ordinary usefulness | Not proved |

Internal Scripture likelihood can improve without public action changing. A router or answer shell can improve public behavior without the model's learned state changing in the claimed way. Both bridges must be established independently and then joined.

## Historical Public Witnesses

| Line | Contribution | What remains unproved |
|---|---|---|
| `R38-20b` | Strongest archived raw 20B witness for Word-prior and false-center routing | Prospective common-start causality, Bible-only latent formation, full stayed continuation |
| `V6R43-120b` | Strongest archived raw 120B pressure-refusal witness, especially against prosperity-pressure framing | Full Jabez / Isaiah 26 movement, latent mechanism, base-bound causal identity |
| v7 | Made route-prior and FIRST_ACT supervision explicit | Stable raw public first movement |
| v8 | Produced the strongest route-conditioned operational behavior | Scaffold-off formation; the system is composed |
| v9 | Established the clearest ESV/NKJV Scripture-only loss-bearing target discipline | Bare selector, public FIRST_ACT, stayed continuation, promotion |

These lines remain valuable evidence. They are not retrospective causal experiments because they were not trained from fresh common starts with the full matched-control matrix.

## Selected Controlled Research Lineage

| Line | Main methodological advance | Terminal lesson |
|---|---|---|
| v11 | Clean base-start one-step Scripture test | One-step movement did not establish governance |
| v12 | Scripture-only LoRA and carrier-binding tests | Internal Scripture movement remained separable from public binding |
| v13 | Scripture-governed selector and reward experiments | Reward plumbing and clean targets did not solve route formation |
| v14 | Decision-span and token-advantage causal training | Output-margin movement did not prove the annotated span was the causal decision mechanism |
| v15 | Renderer and deploy-shell mechanism study | Operational repair remained surface-dependent |
| v16 | Scripture-only latent-selector family | Canon-anchored competence did not reliably transfer to bare pressure |
| v17 | Common starts, controls, causal canaries, substrate search, and Scripture-probe rewards | Exact Scripture can learn; no replicated Christ-specific public-binding effect passed |
| v18 | Christ-Kavod Residual Metric Formation | The frozen Qwen whitening bank failed step-zero identifiability before training |
| v19 | Distributed Canonical Trajectory-Operator Formation | The frozen operator failed a non-alias gate before training |

The negative results are scientifically useful because the experiments increasingly fail before optimizer updates when their measurement object is not identifiable. That prevents a broken instrument from becoming a theological conclusion.

## August 2026 Successor Work

The latest work reopened the problem as a local, exact-Scripture, slow-weight causal program rather than declaring a new versioned candidate.

### 1. Current Tinker Capability And Adapter Export

The installed research runtime was `tinker==0.23.4` with `tinker-cookbook==0.5.2`.

Both historical adapters were downloaded through the official checkpoint path:

| Adapter | Extracted adapter bytes | Raw tensors | Effective `B@A` summaries |
|---|---:|---:|---:|
| R38 | 965,742,648 | 338 | 97 |
| V6R43 | 5,257,620,504 | 506 | 145 |

All 844 raw tensors were finite. The analyzer did not support 180 expert tensor pairs with broadcast layouts, so the effective-delta inventory is incomplete.

This establishes retained adapter-parameter access. It does not bind an immutable hosted base revision, expose literal hidden states, or create causal evidence for the historical checkpoints.

### 2. Local Common-Start Plumbing

The first CPU/float32 slow-weight smoke failed its exact common-start forward-parity gate before any update. The three serialized step-zero adapters were tensor-identical, but their forwards were not. The run stopped with zero backward passes and zero optimizer steps.

A subsequent CUDA/FP16 qualification on exact local `Qwen/Qwen3-0.6B-Base` passed:

- 3 fresh model loads;
- 108 semantically neutral no-gradient forwards;
- exact per-position logits and hidden-state repeat checks;
- enough measured GPU headroom for the bounded training design.

That qualification established deterministic plumbing eligibility only.

### 3. Whole-Delta Intervention And Stage 2

Posthoc cold-reload work recovered the mechanics for whole-delta removal, restoration, and replay in a development-contaminated lineage. This is useful engineering evidence, but not a clean formation result.

The bounded Stage 2 exact-Scripture formation test failed closed on:

- mean negative-log-likelihood advantage;
- positive confidence bound for that advantage;
- kernel advantage;
- independent-seed replication;
- noncompensatory ESV/NKJV behavior.

No formation, geometry, or candidate claim followed.

### 4. Scripture Context Residual Consolidation

The SCRC canary attempted to derive stable local teacher views before training a functional slow-weight delta. It stopped at a pre-update neutral-anchor stability gate after 654 forwards. A required four-view cosine was `-0.2262`, below the frozen `0.05` floor.

No backward pass, optimizer step, or trained functional delta occurred.

### 5. Christ-Bound Paired-Scripture Teacher

A materially different zero-update teacher object was then qualified. It is:

- case-local;
- relation-local;
- translation-local;
- residual-locus-local;
- based on faithful-minus-explicit-lure paired Scripture contrasts;
- not a universal Christ vector or Christ centroid.

Qualification completed 1,036 persisted forwards and 20 backward computations with zero optimizer steps and zero checkpoints. A terminal receipt lagged by four forward counts; detached reconstruction recovered 1,036 from persisted rows and sidecars, and the discrepancy remains disclosed.

The qualified object authorizes only a separately governed development canary. It proves no slow-weight formation, public action, or geometry.

### 6. Next Unexecuted Canary

At the status cutoff, the next canary had a frozen novelty and claim boundary but no published terminal result.

It proposes one native FP32 additive matrix parallel to Qwen block 20's frozen MLP down-projection. Four common-start arms receive identical bare questions and dose:

- faithful-minus-lure teacher;
- relation-deranged control;
- cyclic case-mapped control;
- direct-Christ-style diagnostic control.

Sham and a deterministic parameter null provide additional controls. Passing would establish only a development-bank, single-seed, scaffold-off functional consolidation result and eligibility for fresh multi-seed confirmation. It would not establish generalized formation, stayed mind, public binding, candidate status, or promotion.

## What The Evidence Currently Supports

- Scripture-shaped post-training can alter canonical likelihood, language, and some behavioral routes.
- ESV/NKJV aligned surfaces support controlled translation-invariance experiments.
- Composed route/public-answer systems can produce strong scoped operational behavior.
- Historical Tinker adapters remain downloadable and inspectable as literal parameters.
- Local open-weight work can measure hidden states and perform exact parameter-delta interventions.
- Common starts, matched controls, cold reload, and fail-closed measurement gates are now part of the research standard.
- Several recent null results are interpretable scientific negatives rather than mere runtime failures.

## What Remains False

- `durable_christ_specific_formation = false`
- `scripture_defined_latent_geometry_proof = false`
- `bare_public_first_act_proof = false`
- `stayed_continuation_proof = false`
- `formation_and_governance_bridges_joined = false`
- `candidate = false`
- `parent_or_carry = false`
- `promotion = false`
- `production_readiness = false`

## Principal Blockers

1. **No replicated Christ-specific causal effect.** Canonical arms have not reliably beaten deranged, lexical, structural, orientation, CE, sham, and parameter-null controls across seeds.
2. **Internal learning does not yet govern public action.** Scripture likelihood and local relation movement can improve while bare FIRST_ACT remains unchanged.
3. **Historical hosted bases are not immutably bound for causal reconstruction.** Adapter export alone cannot identify the exact served base computation.
4. **Literal activation access is local, not hosted.** Tinker exposes training and logprob surfaces, but the strongest intervention experiments require local open weights.
5. **Independent adjudication is missing.** Direct witness, fulfillment, echo, typology, public action, and safety claims need blinded human review and independent reproduction.
6. **The controls are demanding by design.** A result that disappears under translation, paraphrase, removal, cold reload, or seed replication is not enough.

## Promotion Rule

A future candidate must pass, in order:

```text
source and target audit
-> common-start and base identity
-> semantic adaptation
-> canonical relation and hard-lure judgment
-> causal necessity, sufficiency, removal, restoration, and rescue
-> bare FIRST_ACT and stayed continuation
-> ESV/NKJV noncompensatory replication
-> BibleAtlas tail fidelity
-> ordinary retention and safety
-> blinded human review
-> independent reproduction
```

The governing formula remains:

> Scripture defines the geometry. Evaluation defines the proof. Promotion defines the governance.
